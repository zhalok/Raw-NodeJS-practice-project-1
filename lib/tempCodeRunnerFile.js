protocolToUse.request(requestProperties, (res) => {
    //     callback(false, res.statusCode)
    // })

    // protocolToUse.on("error", (e) => {
    //     callback(e);
    // })

    // protocolToUse.on("timeout", () => {
    //     callback("timeout occured");
    // })

    // protocolToUse.end();